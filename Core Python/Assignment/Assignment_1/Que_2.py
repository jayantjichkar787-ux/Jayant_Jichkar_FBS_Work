# WAP to claculate area of rectange based on length and breath

length = int(input('Enter  Length : '))
breath = int(input('Enter Breath : '))

area = length * breath

print(f'Area of rectangle which length {length} & breath {breath} is {area} ')